#ifndef PROJECT_H_
#define PROJECT_H_

#include <utility>
#include <vector>

void std_deviator(
	double array[][],
	double z,
	std::vector<std::pair<int, int>>& coordinates
	)

void find_maxima(
	double array[first_index][second_index],
	std::vector<std::pair<int, int>>& coordinates
	)

#endif
